export enum ENUM_ROLES {
  ADMIN = 'admin',
  USER = 'user',
}

export enum ENUM_JOBS {
  sendWebhook = 'sendWebhook',
  sendEmail = 'sendEmail',
}

export enum ENUM_QUEUE {
  newRecord = 'newRecord',
}
